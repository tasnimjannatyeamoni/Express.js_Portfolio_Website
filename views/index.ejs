<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
     
        body {
            font-family: Arial, sans-serif;
            background-color:  rgba(255, 255, 255, 0.5);
            color: #333;
            transition: background-color 0.3s ease;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        header {
            background-color:  rgba(255, 255, 255, 0.5);
            color: #333;
            padding: 20px;
            text-align: center;
            transition: background-color -0.3s ease, color -0.3s ease;
            border-radius: 20px;
            box-shadow: 10px 10px 20px #c0c0c0, -10px -10px 20px #ffffff;
        }

        nav ul {
            list-style-type: none;
            margin: 0;
            padding: 0;
        }

        nav ul li {
            display: inline;
            margin-right: 20px;
            transition: transform 0.3s ease;
        }

        nav ul li a {
            color: #333;
            text-decoration: none;
            border-radius: 20px ;
             padding: 5px 5px;
            transition: color 0.3s ease;
        }

        nav ul li a:hover {
            background-color: rgba(0, 0, 0, 0.1);
        }
        main {
            text-align: center;
            margin: 20px;
            transition: transform 0.3s ease;
        }

        h1 {
            font-size: 36px;
            margin-bottom: 20px;
            color: #333;
            transition: color 0.3s ease;
            animation: floatText 3s ease infinite alternate;
        }

        p {
            font-size: 25px;
            line-height: 1.6;
            margin-bottom: 30px;
            color: #555;
            transition: color 0.3s ease;
            animation: floatText 4s ease infinite alternate-reverse;
        }
        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }

        @keyframes floatText {
            0% {
                transform: translateY(0);
            }
            100% {
                transform: translateY(-10px);
            }
        }

        main {
            animation: fadeIn 1s ease;
        }

        .author-name {
            position: absolute;
            bottom: 10px;
            right: 10px;
            color: #888;
        }

        .profile-picture {
            width: 300px;
            height: 300px;
            border-radius: 50%;
            object-fit: cover;
            box-shadow: 5px 5px 15px rgba(0, 0, 0, 0.2), -5px -5px 15px rgba(255, 255, 255, 0.5);
            transition: transform 0.3s ease;
        }

        .profile-picture:hover {
            transform: scale(1.1);
        }
    </style>
</head>
<body>
    <header>
        <nav>
            <ul>
                <li class="<%= active === 'home' ? 'active' : '' %>"><a href="/">Home</a></li>
                <li class="<%= active === 'about' ? 'active' : '' %>"><a href="/about">About</a></li>
                <li class="<%= active === 'projects' ? 'active' : '' %>"><a href="/projects">Projects</a></li>
                <li class="<%= active === 'services' ? 'active' : '' %>"><a href="/services">Services</a></li>
                <li class="<%= active === 'contact' ? 'active' : '' %>"><a href="/contact">Contact</a></li>
            </ul>
        </nav>
    </header>
    <main>
        <h1>Hello! This is Tasnim Jannat Yeamoni</h1>
        <p>Welcome to my Portfolio Website</p>
        <img src="/image/Yeamoni.jpg" alt="Profile Picture" class="profile-picture">
    </main>

    <div class="author-name">Designed and Developed by Tasnim Jannat Yeamoni</div> 
    
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const header = document.querySelector('header');
            window.addEventListener('scroll', function () {
                if (window.scrollY > 59) {
                    header.style.backgroundColor = '#555';
                } else {
                    header.style.backgroundColor = '#333';
                }
            });
        });
    </script>
</body>
</html>
